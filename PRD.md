# InspiraDB Product Requirements Document

- 产品名称：`InspiraDB`
- 文档版本：`v1.3`
- 更新日期：`2026-03-17`
- 文档目标：定义 InspiraDB V1 的产品范围、核心流程、系统边界、数据模型、验收标准，为产品、设计、工程协同提供统一依据。

## 1. 产品定位与目标

### 1.1 产品定位

`InspiraDB` 是一款面向独立设计师的 macOS 单机个人素材库应用，用于管理设计灵感图、参考图和视觉素材，并通过 AI 分析能力提升检索效率。

### 1.2 核心目标

V1 的目标是建立一条稳定可用的主价值链：

1. 用户导入本地图片文件夹或单张图片。
2. 应用复制图片到应用管理库并完成入库。
3. 系统对图片进行 AI 分析，生成中文描述、中文标签和向量表示。
4. 用户通过中文关键词搜索和标签筛选快速找到目标素材。
5. 用户可查看单张图片的详情、分析状态和元信息，并对标签与描述进行人工修订。

### 1.3 产品目标

- 帮助独立设计师从本地大量素材中快速定位所需参考图。
- 降低人工整理素材、命名素材、回忆素材位置的时间成本。
- 在不依赖多人协作与云端素材托管的前提下，先验证个人素材库的检索价值。

### 1.4 非目标

以下能力不属于 V1 范围：

- 多人协作、权限管理、共享素材库
- XMP 写回与文件元数据同步
- 相似图去重、视觉聚类、自动专辑
- AI 生成设计图、图片填充、Prompt 生成
- Windows 或 Linux 平台支持

## 2. 目标用户与核心场景

### 2.1 目标用户

V1 目标用户为独立设计师，包括但不限于：

- UI/UX 设计师
- 品牌视觉设计师
- 自由职业设计师
- 小型工作室主理人

### 2.2 用户特征

- 主要在 macOS 环境工作。
- 已经积累了大量本地设计图片和参考图。
- 缺少统一的素材命名和分类规范。
- 需要通过语义而不是文件名定位素材。

### 2.3 核心场景

#### 场景一：灵感回查

用户记得某张图的风格或主题，但记不住文件夹位置，希望用中文关键词快速找回。

#### 场景二：素材归档后检索

用户导入多个历史项目的参考图，希望系统自动完成基础整理和打标，以便后续复用。

#### 场景三：按标签缩小结果

用户先用关键词检索，再用标签筛选缩小范围，提高定位效率。

#### 场景四：单张快速收录

用户在浏览素材时临时发现一张值得保留的图片，希望无需准备文件夹，也能直接导入单张图片并立即纳入后续检索。

#### 场景五：人工修订 AI 结果

用户认为 AI 生成的标签或描述不准确，希望在详情页中手动修改内容，使搜索结果更贴近自己的认知。

说明：

- 对单张图片完成标签保存后，该图片的生效标签集合以人工标签为准。

## 3. V1 范围与版本边界

### 3.1 V1 功能范围

V1 包含以下功能：

- 导入本地文件夹中的图片
- 导入单张本地图片
- 将图片复制到应用管理库
- 计算文件 hash 并进行严格重复去重
- 生成缩略图并建立数据库记录
- 将图片加入 AI 分析队列
- 生成中文 caption、中文标签、文本 embedding
- 支持中文关键词搜索
- 支持标签筛选
- 支持查看图片详情与分析状态
- 支持用户手动编辑标签与描述
- 支持删除单张图片
- 支持重新分析单张图片
- 支持失败任务重试

### 3.2 V1 发布节奏

#### V1-A：产品骨架验证版

- 使用 mock AI 结果跑通导入、入库、队列、状态展示、搜索链路。
- 验证数据模型、任务流、UI 状态与主交互。
- 用于内部联调与产品验证，不作为正式发布版本。

#### V1-B：正式发布版

- 接入一个真实 AI 供应商，默认使用智谱。
- 完整支持 AI 分析结果入库与中文检索。
- 对人工编辑内容与 AI 重跑采用固定优先级规则。
- 作为对外可发布的正式版本。

### 3.3 V2 方向

- 评估加入 XMP 手动写回
- 评估支持更多模型供应商
- 评估加入相似图聚类与专辑能力

### 3.4 V3 方向

- 评估本地视觉模型
- 评估跨设备同步
- 评估团队共享能力

## 4. 用户流程

### 4.1 首次使用流程

1. 用户启动应用。
2. 应用展示空状态与导入入口。
3. 用户选择导入文件夹或单张图片。
4. 系统扫描图片并展示预计导入数量或单张确认信息。
5. 用户确认导入。
6. 系统复制文件到应用管理库，完成入库与去重。
7. 系统自动创建分析任务并开始后台处理。
8. 用户可在主界面查看导入进度、分析进度和可搜索结果。

### 4.2 日常搜索流程

1. 用户在搜索栏输入中文关键词。
2. 系统生成查询 embedding 并执行向量召回。
3. 系统结合标签筛选条件过滤结果。
4. 系统按统一排序规则返回结果。
5. 用户点击图片查看详情。

### 4.3 单张编辑流程

1. 用户在结果列表中打开图片详情。
2. 用户查看 AI 生成的描述与标签。
3. 用户手动编辑描述内容或增删标签。
4. 系统保存用户修改并更新搜索所需数据。
5. 修改后的内容优先用于后续展示与检索。

说明：

- 对标签而言，一旦用户完成手动保存，单张图片后续展示与筛选均以人工标签为准。

### 4.4 异常恢复流程

1. 若图片分析失败，图片状态标记为 `failed`。
2. 用户可在列表或详情页看到失败提示。
3. 用户手动触发重试。
4. 系统重新入队并继续处理。
5. 若应用在处理中退出，重启后系统恢复未完成任务。

## 5. 功能需求

### 5.1 导入模块

#### 输入

- 用户选择的本地文件夹路径
- 用户选择的单张图片路径

#### 处理流程

1. 接收文件夹导入或单张导入请求。
2. 扫描目标中的支持图片格式。
3. 过滤非图片文件。
4. 读取文件内容并计算 md5 hash。
5. 检查数据库中是否已存在相同 hash。
6. 对于新图片，复制到应用管理库。
7. 生成缩略图并建立数据库记录。
8. 创建分析任务并入队。

#### 输出

- 成功入库的图片记录
- 重复文件列表
- 导入进度与结果统计
- 单张导入确认结果

#### 失败场景

- 文件夹不存在或无权限访问
- 文件损坏无法读取
- 文件格式不受支持
- 单文件体积超过上限
- 复制失败
- 缩略图生成失败
- 数据库写入失败

#### 用户可见状态

- 空状态
- 扫描中
- 导入中
- 导入完成
- 部分失败
- 单张导入完成

#### 导入边界

- V1 支持的图片格式固定为：`jpg`、`jpeg`、`png`、`webp`、`heic`。
- V1 不支持：`gif`、`svg`、`psd`、RAW 系列格式及其他非静态位图格式。
- 单文件大小上限固定为 `50MB`。
- 不受支持或超出限制的文件应跳过导入，并在导入结果中明确提示。

### 5.2 去重模块

#### 输入

- 图片文件内容

#### 处理流程

1. 对文件计算 md5。
2. 在数据库中查询相同 md5 记录。
3. 若已存在，则标记为严格重复，不重复导入。
4. 若不存在，则继续入库。

#### 输出

- 导入新图片
- 标记重复并跳过

#### 失败场景

- 文件读取失败
- hash 计算失败

#### 用户可见状态

- 导入结果中显示重复数量

### 5.3 AI 分析模块

#### 输入

- 应用管理库中的图片文件

#### 处理流程

1. 图片入库后创建分析任务。
2. 任务从队列中取出后进入 `processing`。
3. 模型生成中文 caption。
4. 基于图片或 caption 生成中文标签。
5. 基于 caption 生成 embedding。
6. 将 caption、标签、embedding 与任务状态写回数据库。
7. 成功后图片状态更新为 `ready`。
8. 若用户后续手动修改描述，则更新检索主文本并触发 embedding 刷新。

#### AI 重跑规则

- 当图片不存在人工描述时，最新成功的 AI 描述可成为生效描述。
- 当图片已存在人工描述时，AI 重跑只写入新的 AI 历史记录，不得覆盖当前人工生效描述。
- 当图片不存在人工标签时，最新成功的 AI 标签可成为生效标签集合。
- 当图片已存在人工标签时，AI 重跑仅更新 AI 建议标签历史，不得覆盖当前人工生效标签集合。

#### 输出

- 中文 caption
- 中文标签
- embedding 向量
- 任务状态结果

#### 失败场景

- 模型请求超时
- 模型返回异常
- 供应商鉴权失败
- 结果解析失败
- 数据库写入失败

#### 用户可见状态

- 等待分析
- 分析中
- 分析完成
- 分析失败
- 可重试

### 5.4 描述编辑模块

#### 输入

- 用户编辑后的中文描述
- 对应图片 ID

#### 处理流程

1. 用户在详情页进入编辑状态。
2. 系统校验输入内容并保存人工描述。
3. 将该描述标记为当前生效描述。
4. 基于最新生效描述重新生成 embedding。
5. 更新图片检索数据与修改时间。

#### 输出

- 最新生效描述
- embedding 刷新结果

#### 失败场景

- 描述保存失败
- embedding 刷新失败
- 图片记录不存在

#### 用户可见状态

- 编辑中
- 保存中
- 保存成功
- 保存失败

#### 保存与检索一致性规则

- 描述保存与 embedding 刷新视为两个连续步骤。
- 若描述保存成功但 embedding 刷新失败，人工描述仍应保存成功并立即用于详情展示。
- 上述场景下，图片进入“待刷新检索”状态，系统需自动补偿重建 embedding。
- 在补偿完成前，搜索结果允许短暂继续使用旧向量，但不得回退人工描述内容。

### 5.5 搜索模块

#### 输入

- 中文文本查询
- 用户选择的标签过滤条件

#### 处理流程

1. 系统将查询文本转换为 embedding。
2. 基于 embedding 对图片向量执行相似度召回。
3. 将标签筛选条件作为附加过滤条件。
4. 统一计算最终排序并返回结果。

#### 输出

- 排序后的图片列表
- 结果总数

#### 失败场景

- 查询 embedding 生成失败
- 向量检索失败
- 数据库查询失败

#### 用户可见状态

- 搜索中
- 有结果
- 无结果
- 搜索失败

### 5.6 标签筛选模块

#### 输入

- 用户已选择的标签集合

#### 处理流程

1. 获取当前查询上下文中的可用标签。
2. 根据用户选择的标签集合过滤结果。
3. 将过滤后的结果返回给列表。

#### 输出

- 已过滤的图片集合

#### 失败场景

- 标签读取失败
- 查询过滤失败

#### 用户可见状态

- 标签加载中
- 标签可选
- 标签为空

### 5.7 标签编辑模块

#### 输入

- 用户新增、删除或调整后的标签集合
- 对应图片 ID

#### 处理流程

1. 用户在详情页编辑标签。
2. 系统校验标签内容并标准化存储。
3. 建立或更新图片与标签的关系。
4. 将人工标签设为该图片的唯一生效标签集合。
5. 更新图片检索相关数据与修改时间。

#### 输出

- 最新生效标签集合

#### 失败场景

- 标签保存失败
- 标签关联更新失败
- 图片记录不存在

#### 用户可见状态

- 编辑中
- 保存中
- 保存成功
- 保存失败

#### 人工标签锁定规则

- 用户首次保存人工标签后，该图片的 `active_tag_source` 切换为 `user`。
- 当 `active_tag_source = user` 时，后续 AI 重跑不得改变该图片的生效标签集合。
- V1 不提供“恢复为 AI 标签”的自动切换逻辑，仅允许后续版本评估。

### 5.8 图片详情模块

#### 输入

- 用户点击的图片 ID

#### 处理流程

1. 读取图片基础信息。
2. 读取 caption、标签、分析状态。
3. 提供描述与标签编辑入口。
4. 展示导入时间、来源路径、库内路径等元信息。

#### 输出

- 详情面板信息

#### 失败场景

- 图片记录不存在
- 详情数据读取失败

#### 用户可见状态

- 加载中
- 展示详情
- 加载失败

### 5.9 图片维护模块

#### 输入

- 用户选择的图片 ID
- 用户触发的维护动作：`delete` 或 `reanalyze`

#### 处理流程

1. 系统读取目标图片记录。
2. 若动作为 `delete`，则删除应用管理库中的图片文件、缩略图与关联衍生数据。
3. 若动作为 `reanalyze`，则创建新的分析任务并重新执行 AI 分析。
4. 若图片存在人工描述或人工标签，则 AI 重跑遵循人工优先覆盖规则。

#### 输出

- 删除结果
- 重新分析结果

#### 失败场景

- 图片不存在
- 文件删除失败
- 数据清理失败
- 重分析任务创建失败

#### 用户可见状态

- 删除中
- 删除成功
- 删除失败
- 重新分析中
- 重新分析成功
- 重新分析失败

## 6. 数据模型

本节定义 V1 的核心实体与职责。字段名为产品与工程对齐用的逻辑字段，不等同于最终 SQL 实现。

### 6.1 `images`

用途：存储图片主记录。

核心字段：

- `id`
- `original_file_name`
- `source_path`
- `library_path`
- `thumbnail_path`
- `md5_hash`
- `file_size`
- `width`
- `height`
- `import_status`
- `analysis_status`
- `active_caption_id`
- `active_tag_source`
- `created_at`
- `updated_at`

说明：

- `source_path` 记录导入来源。
- `library_path` 记录应用库内文件路径。
- `analysis_status` 用于驱动列表和详情页展示。
- `active_caption_id` 指向当前用于展示与检索的生效描述。
- `active_tag_source` 用于标记当前生效标签来自 `ai` 或 `user`。

### 6.2 `captions`

用途：存储图片的中文描述，包括 AI 生成描述和人工修订描述。

核心字段：

- `id`
- `image_id`
- `content`
- `source`
- `is_active`
- `model_provider`
- `model_name`
- `created_at`
- `updated_at`

说明：

- `source` 至少支持 `ai` 与 `user`。
- 同一时间仅允许一条描述记录处于 `is_active = true`。

### 6.3 `tags`

用途：存储标准化标签。

核心字段：

- `id`
- `name`
- `language`
- `created_at`

说明：

- V1 默认为中文标签。
- 相同标签在逻辑层面应避免重复创建。

### 6.4 `image_tags`

用途：存储图片与标签的关联关系。

核心字段：

- `id`
- `image_id`
- `tag_id`
- `source`
- `created_at`

说明：

- `source` 用于标记标签来源，V1 至少支持 `ai` 与 `user`。
- 对单张图片，若存在人工保存标签，则人工标签为唯一生效标签集合。
- AI 标签可保留为初始建议或历史记录，但不参与该图片后续展示与筛选。

### 6.5 `embeddings`

用途：存储图片对应的向量记录。

核心字段：

- `id`
- `image_id`
- `vector`
- `dimension`
- `model_provider`
- `model_name`
- `created_at`

说明：

- embedding 由 caption 文本生成。
- V1 每张图片只保留一份主 embedding 记录。

### 6.6 `analysis_jobs`

用途：存储分析任务与失败信息。

核心字段：

- `id`
- `image_id`
- `job_type`
- `status`
- `retry_count`
- `max_retry_count`
- `last_error_code`
- `last_error_message`
- `started_at`
- `finished_at`
- `created_at`
- `updated_at`

说明：

- 用于恢复任务、失败追踪、手动重试。

### 6.7 `app_settings`

用途：存储应用级配置。

核心字段：

- `id`
- `api_provider`
- `api_key_ref`
- `cloud_analysis_enabled`
- `library_root_path`
- `created_at`
- `updated_at`

说明：

- `api_key_ref` 指向系统安全存储中的引用，而不是明文。

## 7. 系统架构

### 7.1 架构概览

```text
Electron Main Process
├── 文件扫描与复制
├── 应用管理库
├── SQLite 数据库
├── 向量检索引擎
├── 缩略图生成
├── AI 服务调用
└── 任务队列与失败恢复

Renderer Process
├── 主界面列表
├── 搜索栏
├── 标签筛选面板
├── 图片详情面板
├── 导入与分析状态展示
└── 标签与描述编辑交互
```

### 7.2 技术边界

- 桌面容器：Electron
- 前端：React
- 组件库：shadcn/ui
- 样式方案：Tailwind CSS
- 状态管理：Zustand
- 本地数据库：SQLite
- 向量检索：SQLite 向量扩展方案
- AI 服务：V1-B 默认接入智谱

### 7.3 设计系统与视觉风格

- UI 基础组件统一采用 `shadcn/ui`，避免自建基础控件造成交互与样式不一致。
- 整体视觉风格定义为禅意设计风格，强调极简、克制、安静、精细化。
- 布局应保留充分留白，避免拥挤的信息堆叠。
- 色彩以低饱和中性色为主，交互高亮应克制使用。
- 卡片、面板、输入区与弹层应使用统一的圆角、边框与阴影体系。
- 动效应短促且轻量，不得喧宾夺主。
- 视觉语言需优先服务于浏览、筛选、阅读和编辑，不追求装饰性。

### 7.4 存储策略

- 导入图片后复制到应用管理库。
- 应用仅管理库内副本，不修改原始来源文件。
- 原始路径仅作为来源信息保留。
- V1 不写回 XMP，不修改原始文件元数据。

## 8. 任务队列与失败恢复

### 8.1 图片状态

图片状态定义如下：

- `imported`：已完成入库，尚未排队
- `queued`：已进入分析队列
- `analyzing`：正在执行分析
- `ready`：分析完成，可被检索
- `failed`：分析失败，等待用户重试

### 8.2 任务状态

任务状态定义如下：

- `pending`
- `processing`
- `succeeded`
- `failed`
- `retrying`

### 8.3 队列规则

- 导入完成后自动创建分析任务。
- 队列默认并发数固定为 `3`，避免 UI 阻塞与过量模型请求。
- 单任务超时时间固定为 `60s`。
- 每个任务最大重试次数固定为 `2`。
- 失败重试退避时间固定为：第一次 `30s`，第二次 `120s`。
- 失败任务应记录错误信息。
- 用户可对失败任务手动发起重试。

### 8.4 应用重启恢复规则

- 启动时扫描 `pending`、`processing`、`retrying` 状态任务。
- 对异常中断的 `processing` 任务重新入队。
- 已完成任务不重复执行。
- 图片主记录与任务状态必须保持一致。

### 8.5 失败可见性要求

- 列表中可见图片分析失败状态。
- 详情页可见失败原因摘要。
- 用户可以执行重试。

## 9. 搜索规则与排序策略

### 9.1 检索语言

- V1 检索语言为中文优先。
- 中文标签、中文 caption、中文搜索词构成主链路。

### 9.2 搜索行为

1. 用户输入中文关键词。
2. 系统调用 embedding 服务生成查询向量。
3. 系统执行向量召回获取候选结果。
4. 若用户选择标签，则标签筛选按 `AND` 逻辑执行。
5. 系统统一排序后返回结果。

说明：

- 当用户手动修改描述后，应以人工生效描述作为 embedding 的主文本来源。
- 当用户手动修改标签后，该图片的人工标签作为唯一生效标签参与展示与筛选。
- AI 标签不再参与该图片的正式筛选结果，仅保留为历史建议数据。
- 当查询词为空且未选择标签时，系统默认返回最近导入的 `ready` 状态图片。
- 当查询词为空但已选择标签时，系统按标签条件过滤 `ready` 状态图片。

### 9.3 排序规则

- V1 由后端统一生成排序结果。
- 前端不自行合并或重算向量分数。
- 排序目标为相关性优先。
- 当候选结果相关性接近时，可按导入时间做次级排序。

### 9.4 结果范围

- 单次搜索默认返回首批 `50` 条结果。
- 分页大小固定为 `50`。
- 列表默认按分页或分批加载实现。
- `ready` 状态图片方可进入正式搜索结果。
- 同分结果按 `updated_at` 倒序作为次级排序规则。

## 10. 非功能需求

### 10.1 平台要求

- V1 仅支持 macOS。

### 10.2 交互与视觉要求

- 主界面、详情页、搜索栏、筛选面板与弹层统一基于 `shadcn/ui` 组件体系构建。
- 禅意风格需落到真实界面细节，包括留白、对齐、字号层级、边框节奏和交互反馈。
- 编辑标签与描述时，应保持低干扰的交互方式，不打断用户浏览流程。

### 10.3 性能要求

- 导入过程不得阻塞主界面基本交互。
- V1-B 的目标测试样本规模固定为 `5,000` 张已入库图片。
- 在 `5,000` 张图片样本下，搜索首屏结果返回时间目标不高于 `1.5s`。
- 在 `5,000` 张图片样本下，应用冷启动进入可操作主界面的时间目标不高于 `5s`。
- 应用异常重启后，应在 `30s` 内恢复未完成任务扫描并重新入队。

### 10.4 数据一致性要求

- 数据库记录、应用管理库文件、缩略图文件状态必须一致。
- 图片复制成功后方可建立可用记录。
- AI 分析结果写入必须与任务状态更新保持一致。
- 用户编辑描述或标签后，详情展示、列表展示与搜索数据必须保持一致。

### 10.5 隐私与安全要求

- 首次启用云分析前应明确提示图片会发送给云模型供应商处理。
- 用户可关闭云分析能力。
- API Key 不以明文写入普通业务表。
- 应优先使用系统安全存储能力保存密钥引用。

### 10.6 日志与错误记录

- 记录导入失败、AI 调用失败、数据库异常、任务恢复信息。
- 错误日志用于调试与问题复现。
- 日志记录不得泄露完整敏感密钥。

## 11. 版本规划

### 11.1 V1-A

- 完成导入、复制、去重、缩略图、数据库落库
- 完成任务队列、状态展示、失败恢复
- 完成单张导入与详情页编辑骨架
- 使用 mock 数据跑通搜索链路

### 11.2 V1-B

- 接入智谱真实模型
- 完成 caption、标签、embedding 的真实生成
- 完成中文检索与标签筛选
- 完成用户编辑标签与描述后的检索更新
- 完成图片删除与重新分析链路
- 形成可发布版本

### 11.3 V2

- 评估 XMP 手动写回
- 评估更多模型供应商
- 评估更丰富的标签管理能力

### 11.4 V3

- 评估本地模型
- 评估协作与同步能力

## 12. 验收标准

### 12.1 功能验收

以下条件全部满足时，V1-B 可视为功能达标：

- 用户可以成功导入本地图片文件夹。
- 用户可以成功导入单张本地图片。
- 不受支持格式与超限文件会被明确跳过并提示原因。
- 图片会被复制到应用管理库。
- 系统可识别并跳过严格重复文件。
- 新导入图片会自动进入分析队列。
- 分析成功后可生成中文描述、中文标签和 embedding。
- 用户可以通过中文关键词搜索找到结果。
- 用户可以通过标签筛选缩小结果范围。
- 用户可以打开图片详情查看元信息和分析状态。
- 用户可以在详情页编辑标签与描述，并保存成功。
- 编辑后的描述与标签可反映到后续检索结果中。
- 对已人工编辑标签的单张图片，搜索与筛选结果以人工标签为准。
- 对已人工编辑描述的图片，AI 重跑不得覆盖人工生效描述。
- 用户可以删除单张图片，系统会同步清理关联衍生数据。
- 用户可以重新分析单张图片，且人工优先规则保持生效。
- 分析失败的图片可见且可重试。

### 12.2 质量验收

- 文档中的 V1 范围与非目标保持一致，无互相冲突项。
- 数据模型、状态机、搜索规则、版本规划之间保持一致。
- 不存在未拍板的核心技术分叉表述。
- 正式文档中不包含鼓励性口语表述、推荐语和营销式描述。

### 12.3 非功能验收

- 在 `5,000` 张图片样本下，主界面在导入与分析过程中仍可交互。
- 在 `5,000` 张图片样本下，搜索首屏结果返回时间不高于 `1.5s`。
- 应用异常退出后，未完成任务可在 `30s` 内恢复扫描与重新入队。
- 日志中可追踪主要失败原因。
- 用户可明确感知云分析行为并进行控制。

## 13. 内部能力接口

以下接口为产品与工程对齐用的逻辑接口：

- `importFolder(folderPath)`
  - 扫描并导入文件夹中的图片。
- `importFile(filePath)`
  - 导入单张显式选择的图片文件。
- `enqueueAnalysis(imageId)`
  - 为指定图片创建并加入分析任务。
- `retryAnalysis(jobId)`
  - 重新执行失败任务。
- `searchImages(query, selectedTags, pagination)`
  - 执行中文搜索与标签筛选。
- `getImageDetail(imageId)`
  - 获取详情页所需信息。
- `updateImageCaption(imageId, content)`
  - 更新图片的生效描述并刷新检索文本。
- `updateImageTags(imageId, tags)`
  - 更新图片的生效标签集合。
- `deleteImage(imageId)`
  - 删除图片及其关联衍生数据。
- `rebuildImageAnalysis(imageId)`
  - 为指定图片重新创建分析任务。

## 14. 默认约束与实现假设

- 目标用户固定为独立设计师。
- 首发平台固定为 macOS。
- 首发版本固定为单机应用，不包含云端素材托管。
- V1 不写回 XMP。
- 导入后复制到应用管理库，不直接依赖原始路径运行。
- V1 同时支持文件夹导入与单张图片导入。
- 去重仅做严格重复去重，不做视觉相似去重。
- 用户可人工修改标签与描述；其中单张图片的标签以人工结果为准。
- 若图片存在人工描述，则 AI 重跑不得覆盖该人工生效描述。
- 标签筛选固定采用 `AND` 逻辑。
- 空查询默认返回最近导入的 `ready` 状态图片。
- V1-A 使用 mock AI，V1-B 使用真实 AI。
- 真实 AI 供应商默认先接入智谱，MiniMax 保留为后续扩展位。
